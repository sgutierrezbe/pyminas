# Pruebas

Inicia `python3 server.py` desde la raíz y abre:

```text
http://localhost:8080/tests/test_runner.html
```

La suite carga la aplicación y el currículo desde la raíz mediante rutas relativas.
